# Screening campaigns export — EMAcross D1 + H4 — 2026-07-22

## Status: SUCCESS

## Approach (security constraint respected)

- `apex-compute` never received a Supabase credential. Artifacts were rsynced *from* the box
  (report_EMAcross_D1.json, report_EMAcross_H4.json ~68MB, null_deep_EMAcross_H4.json) to this
  workstation's temp dir via `wsl -- rsync` run through **PowerShell** (the Bash tool's Git-Bash
  path mangling broke the first attempt — `-e "ssh -F /mnt/c/..."` got rewritten to a Windows
  path and rsync failed with "source and destination cannot both be remote"; PowerShell avoided it).
- Checked out branch `session/export-nassets-0722` (already current in the vault worktree) and
  imported `build_campaign_payload` from
  `D:\brain-vault\projects\apex-trading\backtests_massive\scripts\export_screening_to_site.py`
  rather than reimplementing it. Build step: `.../scratchpad/build_payloads.py`.
- Credentials (`NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`)
  were read only from `D:\code\algoproof\.env.local`, only on this Windows machine, only by
  throwaway scripts in the system temp scratchpad
  (`.../scratchpad/insert_screening.mjs`, `.../scratchpad/verify_screening.mjs`) — never
  committed, never copied anywhere.
- Python `requests` hit this machine's known Avast-SSL-interception issue
  (`CERTIFICATE_VERIFY_FAILED`, same class as `feedback_python_windows_ssl_avast.md`). Per the
  task's own fallback instruction ("install nothing globally — use node with fetch instead"),
  switched to `node --version` v24.14.0 with native `fetch` against the PostgREST endpoint. No
  packages installed anywhere.
- Neither repo's tracked files were modified. No commits made.

## Payloads built (matched the expected numbers exactly)

- EMAcross/D1: n_behaviors=374, n_rejected=374, n_marginal=0, n_candidates=0, n_assets=30
- EMAcross/H4: n_behaviors=73770, n_rejected=73744, n_marginal=24, n_candidates=2, n_assets=30
  - candidate A: null_pct=95.16
  - candidate B: null_pct=98.66

## Step 3 — insert (service role key, Node/fetch)

```
D1 campaign row: 1 EMAcross D1
  candidates: 0
H4 campaign row: 2 EMAcross H4
  candidates: 2: [ [ 'A', 95.16 ], [ 'B', 98.66 ] ]
event inserted: 1 EMAcross D1 campaign_closed
event inserted: 2 EMAcross H4 campaign_closed
event inserted: 3 EMAcross H4 remeasure
```

## Step 4 — verify with ANON key (RLS-scoped, as the site reads)

```
=== screening_campaigns (ANON) ===
  EMAcross/D1: state=judged judged_on=2026-07-21 n_behaviors=374 n_rejected=374 n_marginal=0 n_candidates=0 n_assets=30
  EMAcross/H4: state=judged judged_on=2026-07-22 n_behaviors=73770 n_rejected=73744 n_marginal=24 n_candidates=2 n_assets=30
=== screening_candidates for H4 (ANON) ===
  label=A rank=1 null_pct=95.16 qualified_assets=[] bot_slug=null
  label=B rank=2 null_pct=98.66 qualified_assets=[] bot_slug=null
=== screening_events (ANON) ===
  D1 2026-07-21 campaign_closed: Campagne close — 374 configurations jugées, aucune ne tient. La machine sait dire non.
  H4 2026-07-22 campaign_closed: Campagne close — 73 770 configurations jugées, 6 candidates au premier comptage.
  H4 2026-07-22 remeasure: Re-mesure du test de hasard : 100 tirages ne suffisaient pas, je suis passé à 5 000. Quatre des six candidates étaient du bruit de mesure. Il en reste deux.
```

Confirmed under RLS (public read policies `... for select using (true)` on all three tables,
`supabase/migrations/014_screening.sql`): two campaign rows with the expected counts and
`n_assets = 30`; two H4 candidate rows with the right percentiles and labels A/B; three event
rows, exact wording preserved. No RLS gap — the ANON read matches the service-role write 1:1.

## Step 5 — what the site will render

**`/strategies`** (`ScreeningGrid` via `getScreeningGrid()` — now returns 2 rows, was empty):
- Corpus band: "74 144 configurations jugées (1 stratégie)" · "74 118 rejetées" · "2 en observation".
- EMAcross row block, one row per TF_ORDER entry:
  - D1: 374 jugées · count "0" · état "clos · résultat négatif · 21/07/2026"
  - H4: 73 770 jugées · count "2" · état "2 en observation · 22/07/2026"
  - H1 / M30 / M15 / M5: no row in DB → "—" / "—" / "jamais testé" (unaffected by this export)

**`/strategies/famille/EMAcross`** (`getStrategyDossier` — 404'd before, now renders):
- D1 dossier: funnel 374 jugées → 374 rejetées, 0 marginales, 0 debout; renders the zero-candidate
  copy ("Aucune configuration ne tient... la machine sait dire non, ... 374 fois sur 374"). No
  cross-TF note (D1 itself has nothing standing).
- H4 dossier: funnel 73 770 jugées → 73 744 rejetées, 24 marginales, 2 debout. Cross-TF note fires
  as **"isolated"** (H4 stands alone; neither D1 — 0 candidates — nor H1 — no campaign at all — has
  anything standing), showing the fragility copy. Candidate A card: null 95,16 vs bar 95 → **"un
  souffle"** tag (margin 0.16 ≤ 2). Candidate B card: null 98,66 vs bar 95 → no tag (margin 3.66 > 2).
  Both cards show "0 trade forward" (forward_trades=0, no bot_slug yet).
- Journal des verdicts: all 3 event rows render, newest first (22/07 ×2, then 21/07).

**Assets exhibit — explicitly suppressed, correctly.** `showAssetsExhibit` requires
`candidates.length === 2 && candidates[0].qualified_assets.length > 0 && candidates[1]…length > 0`.
Both H4 candidates were inserted with `qualified_assets: []` (exporter doesn't populate it yet), so
the condition is false and the "Pièce à conviction" aside does **not** render. This is the correct,
safe degrade per the component's own guard — not a bug, and nothing needs fixing for this export.
